extend type Mutation {
    createGroup(requestGroup:JSON,photo:Upload):responseGroup @field(resolver: "GroupMutations@create")
    updateGroup(requestGroup:JSON,photo:Upload):responseGroup @field(resolver: "GroupMutations@update")
}

type responseGroup {
    message: String
    group: Group
}

#import Group.graphql
