extend type Mutation {
    createContact(settingRequest:JSON!): responseContact @field(resolver: "ContactoMutations@create")

}

type responseContact{
    message: String!
    contacts: Setting
}

#import contact.graphql
